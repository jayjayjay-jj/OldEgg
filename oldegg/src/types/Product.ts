type Product = {
    name? :string,
    category_id?: Number,
    shop_id?: Number,
    image?: string,
    description?: string,
    price?: Number,
    stock?: Number,
    details?: string
}

export default Product